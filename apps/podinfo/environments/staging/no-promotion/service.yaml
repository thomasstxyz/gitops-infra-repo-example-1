apiVersion: v1
kind: Service
metadata:
  name: podinfo
spec:
  # type: ClusterIP
  type: LoadBalancer
  selector:
    app: podinfo
  ports:
    - name: http
      port: 29898
      protocol: TCP
      targetPort: http
    - port: 29999
      targetPort: grpc
      protocol: TCP
      name: grpc